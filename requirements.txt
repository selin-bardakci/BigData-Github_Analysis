# =============================================================================
# requirements.txt
# Python dependencies for local development and notebook work.
# Spark jobs running on Dataproc use the cluster's pre-installed PySpark.
# Install locally with:  pip install -r requirements.txt
# =============================================================================

# ---------- Distributed processing (local testing only) ----------
pyspark==3.4.4

# ---------- GCP clients ----------
google-cloud-bigquery==3.25.0
google-cloud-storage==2.18.2
db-dtypes==1.3.0             # required by BigQuery pandas integration

# ---------- Data handling ----------
pandas==2.2.2
numpy==1.26.4
pyarrow==16.1.0              # Parquet read/write

# ---------- Machine learning ----------
scikit-learn==1.5.1
statsmodels==0.14.2          # ARIMA / SARIMAX
prophet==1.1.5               # Facebook/Meta time-series forecasting

# ---------- Graph algorithms ----------
networkx==3.3
python-igraph==0.11.6        # Louvain / Leiden community detection
leidenalg==0.10.2            # Leiden algorithm (improved Louvain)
# Node2Vec embeddings: implemented manually in notebooks using networkx random walks
# + scikit-learn TruncatedSVD (no separate package needed)

# ---------- Visualization ----------
matplotlib==3.9.1
seaborn==0.13.2
plotly==5.22.0               # interactive Sankey and alluvial charts

# ---------- Notebook ----------
jupyter==1.0.0
ipykernel==6.29.5
